# Environment variables for upload-service
# Copy this file to .env and update values as needed
# Do not commit .env to version control

# JWT secret key (generate with: openssl rand -hex 32)
SECRET_KEY="OvwHhIjs|)os0LO0bgMh0f>;(k75A4N={s`V&'[u'`OEih)YWd"

# MinIO credentials
MINIO_ROOT_USER=minio_user
MINIO_ROOT_PASSWORD=secure_password123

# MinIO external host for file URLs (e.g., localhost:9000 for dev, minio.example.com:9000 for prod)
MINIO_EXTERNAL_HOST=localhost:9000